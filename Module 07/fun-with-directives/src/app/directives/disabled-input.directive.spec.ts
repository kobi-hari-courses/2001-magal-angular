import { DisabledInputDirective } from './disabled-input.directive';

describe('DisabledInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DisabledInputDirective();
    expect(directive).toBeTruthy();
  });
});
